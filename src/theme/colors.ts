import { gray, slate, tomato, green, indigo, whiteA, blackA } from '@radix-ui/colors';

const colors = {
  ...indigo,
  ...slate,
  ...green,
  ...tomato,
  ...gray,
  ...whiteA,
  ...blackA,

  // black and white
  white: 'white',
  black: 'black',
};

export default colors;
