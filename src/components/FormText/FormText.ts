import { styled } from '../../stitches.config';
import { StyledText } from '../Text';

export const FormText = styled(StyledText, {
  defaultVariants: {
    size: 'xs',
    variant: 'muted',
  },
});

export default FormText;
